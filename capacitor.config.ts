import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_Academic2.0',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
